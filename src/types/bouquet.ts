export type BouquetAsset = {
  id: string;
  label: string;
  src: string;
};

export type BouquetDraft = {
  backgroundIds: string[];
  flowerIds: string[];
  cardTitle: string;
  cardMessage: string;
};
